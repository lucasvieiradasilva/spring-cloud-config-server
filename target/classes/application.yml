server:
   port: 8888
      
spring:
   profiles:
      active: native
   cloud:
      config:
         server:
            native:
               search-locations: F:/Workspaces/eclipse/spring-cloud-config-folder
               